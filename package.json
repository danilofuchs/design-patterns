{
  "name": "design-patterns",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "yarn test:mocha",
    "test:compile": "tsc --noEmit",
    "test:mocha": "ts-mocha 'src/**/*.ts' 'test/**/*.ts' --paths --config .mocharc.json",
    "test:watch": "yarn test:mocha --watch -R min"
  },
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "@types/node": "^14.0.1",
    "chai": "^4.2.0",
    "dotenv": "^8.2.0",
    "mocha": "^7.1.2",
    "ts-mocha": "^7.0.0",
    "ts-node": "^8.10.1",
    "typescript": "^3.9.2"
  }
}
